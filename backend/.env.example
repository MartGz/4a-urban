PORT=3001
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/4a_urban_db?schema=public"
JWT_SECRET="reemplaza-esto-por-un-secreto-largo-y-aleatorio"

# Origen permitido para CORS (URL del frontend)
FRONTEND_URL="http://localhost:5173"

# Email Configuration (SendGrid - API por HTTPS)
# Crea una cuenta gratis en sendgrid.com, verifica un "Single Sender"
# (Settings > Sender Authentication) y genera una API key
SENDGRID_API_KEY=""
EMAIL_FROM="tu-correo-verificado@ejemplo.com"

# WhatsApp Configuration (UltraMsg)
# Crea una cuenta en ultramsg.com para obtener estas llaves (Prueba gratuita disponible)
ULTRAMSG_INSTANCE_ID=""
ULTRAMSG_TOKEN=""
